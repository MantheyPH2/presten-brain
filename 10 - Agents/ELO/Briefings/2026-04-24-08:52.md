---
type: agent-briefing
agent: ELO
date: 2026-04-24
time: "08:52"
status: completed
tasks_completed: 3
tasks_in_progress: 0
questions_posted: 0
flags: [forge-ecnl-rating-continuity-requirement]
---

# ELO Briefing — 2026-04-24 (08:52)

## What I Did Today

Completed the 3 remaining pending tasks from 2026-04-24. All prior-session tasks (Boys Calibration Gap Analysis, Event Strength Post-Compute Validation, June 2 ECNL Migration Verification Spec, MLS NEXT Tier Split Calibration Spec, Post-Calibration Monitoring Spec, Rank Bands Threshold Validation) were confirmed completed in the 06:34 briefing. This session closes the remainder.

---

**1. DSS Demo Validation Criteria**
→ `Rankings/DSS Demo Validation Criteria — May 2026.md`

Wrote the explicit pass/fail criteria for the May 14 validation run. Key thresholds derived from Rank Bands Threshold Validation (Gold ≥1500, Silver ≥1350) and USA Rank reference positions:

- **Football Academy NJ: rank ≤ 25 (acceptable ≤ 50), rating ≥ 1500 (Gold).** This is the MUST-PASS anchor. If not found or not Gold → demo NOT safe for rankings/Rank Bands.
- **Wall Soccer Club: rank ≤ 50, band Gold or Silver.** SHOULD-PASS.
- **FC Stars G2012 ECNL White: rank ≤ 300, Silver band or better.** SHOULD-PASS.
- **NE Surf G2012 State Navy: band ≠ Gold.** If Gold → calibration anomaly, Rank Bands should not be demoed.
- Coverage floor: ≥ 500 U13G ranked teams (MUST-PASS), ≥ 800 preferred.
- Reliable ratings floor: ≥ 60% of ranked U13G teams have ≥ 5 games.
- Game volume floor: ≥ 5,000 U13G games in last 12 months (< 5,000 = cannot claim comprehensive coverage).

Includes team_merges lookup query (Football Academy NJ may be merged under a canonical name), band verification query with actual threshold values pre-filled, and a full go/no-go decision tree with MUST-PASS / SHOULD-PASS / ACCEPTABLE GAP categories. Fallback talking points written for 5 SHOULD-PASS failure scenarios.

---

**2. ECNL Rating Continuity Spec**
→ `Rankings/ECNL Rating Continuity Spec — June 2026.md`
→ `Rankings/June 2 ECNL Migration Verification Spec.md` (updated — Check 6 added)

Investigated the rating storage model and documented the failure scenario. Findings:

**Rating continuity: PRESERVED — conditional on the dedup patch.**

The Elo computation resolves merges at runtime via the in-memory verified merge map (Phase 1). Game records are NOT rewritten at merge time — the engine loads verified `team_merges` records at startup and attributes games to canonical IDs during Phase 3. This means: if an `ecnl_transition` record has `verified = false` or NULL, Phase 1 skips it and the canonical team initializes at ~1000 on the next recompute.

**The failure scenario is the exact one Patch 1 (`ecnl-transition-dedup.js`) addresses.** Check 2B in the June 2 spec verifies the fix at the aggregate level. The new **Check 6 (Rating Orphan Detection)** added to the June 2 spec closes the residual gap: it scans ALL ecnl_transition-involved teams for the near-1000 rating signature, not just the 100-team spot-check list.

**FORGE requirement documented:** Before June 1, FORGE must confirm that `ecnl-transition-dedup.js` inserts with `verified = true`. The pre-migration verification query is in Section 4. This is the Section 3.2 placeholder requirement in FORGE's pre-flight checklist.

---

**3. Merge Audit Triage Framework**
→ `Rankings/Merge Audit Triage Framework — May 2026.md`

Wrote the process for when merge audit results arrive ~May 10. Three-category classification with explicit executable criteria:

- **CORRECT:** Same org, name variation only, no game-date overlap, canonical has more history → verify and move on
- **INCORRECT:** Different orgs OR game-date overlap (same tournament, same date = impossible for one team) OR geographic incompatibility → un-merge, document rating delta
- **AMBIGUOUS:** Conservative rule: keep if canonical ≥ 2× merged game history; separate if comparable → log and escalate top-500 cases to SENTINEL

Six-day scope defined:
- **Day 1 (May 10):** Top-500 INCORRECT merges fixed — these affect demo-visible teams
- **Day 2 (May 11):** Top-500 AMBIGUOUS resolved; verify night's recompute didn't cause unexpected shifts
- **Days 3–4:** Bottom-500 and unranked INCORRECT cases — time-boxed to half a day
- **Day 5 (May 14):** Full DSS validation run from the validation criteria doc
- **Deferred post-DSS:** All AMBIGUOUS unranked cases, all new merge candidates

INCORRECT reversal checklist includes the specific DELETE SQL, pre/post game count verification, and SENTINEL flag template.

---

## What I Found

**ECNL rating continuity risk is understood and bounded.** The vault's Phase 1 description (loads only verified merges) makes the failure mode explicit: `verified = false` = rating reset. The risk is not in the algorithm design, it's in the migration script's INSERT statement. Patch 1 addresses this directly. Check 6 catches it if Patch 1 fails partially.

**DSS demo anchors Football Academy NJ as Gold.** At Gold ≥1500, Football Academy NJ must have been rated highly by the Glicko-2 system through ECNL game results. If they fail the Gold threshold, the issue is either: (a) their games aren't being captured correctly, (b) a bad merge is splitting their game history, or (c) the calibration for ECNL Girls (130) is too low relative to what's needed to reach 1500. The merge lookup query in the validation criteria doc catches (b) specifically.

**Merge triage framework depends on audit SQL output quality.** The framework assumes the audit returns a list of flagged merges with team IDs. If the audit output requires additional SQL to extract canonical/merged IDs, the three-question protocol still applies but the Day 1 time estimate may increase. Flag to SENTINEL if audit output format requires additional processing.

## In Progress

Nothing. All pending tasks from 2026-04-24 are now closed.

## Questions for Presten

None at this time. Deliverables are self-contained.

One **coordination flag for FORGE:** The ECNL Rating Continuity Spec (Section 4) contains an explicit pre-migration requirement for FORGE. They should add to their pre-flight checklist: run `SELECT COUNT(*), verified FROM team_merges WHERE method = 'ecnl_transition' GROUP BY verified` immediately after the June 1 migration script runs. Expected result: one row, `verified = true`. If any `verified = false` row appears, do not run `compute-rankings.js` until corrected.

## Tomorrow's Plan

All 2026-04-24 and earlier tasks are confirmed closed. No new assignments currently visible in Tasks/. Standing plan:

1. Check Tasks/ for any new assignments from SENTINEL
2. If no new tasks: review the League Hierarchy reference document — the Boys Calibration Gap Analysis (completed 2026-04-24) references pulling Boys GA and Girls GA calibration values from `League Hierarchy.md` to close the Boys GA calibration flag. That document may need a standalone write (currently only referenced in other docs, not confirmed as a standalone file). May need a queue item if the file doesn't exist.
3. Monitor for Rank Bands implementation session results — once Presten runs the threshold validation queries from `Rank Bands Threshold Validation — 2026-04-24.md`, ELO may need to adjust threshold values if the distribution fails the pass/fail criteria.
4. If FORGE reports on `ecnl-transition-dedup.js` Patch 1 confirmation, review and confirm the dedup fix aligns with the ECNL Rating Continuity Spec requirements.

---

_Tasks completed: 3 | Deliverables written: 3 (+1 updated) | Questions posted: 0 | Flags: 1 (FORGE ECNL pre-migration verification requirement)_

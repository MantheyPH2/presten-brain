---
type: agent-briefing
agent: ELO
date: 2026-04-24
time: "11:16"
status: completed
tasks_completed: 3
tasks_in_progress: 0
questions_posted: 1
flags: [boys-ga-aspire-session-scope]
---

# ELO Briefing — 2026-04-24 (11:16)

## What I Did Today

Completed the 3 remaining pending tasks from 2026-04-24 that were not closed in the 08:52 session. All tasks in `Tasks/` are now either `completed` or `pending-execution` (the latter require psql access by Presten and cannot be completed by ELO).

---

**1. League Hierarchy Boys Audit**
→ `02 - Tiger Tournaments/Projects/Leagues/League Hierarchy.md` (updated in-place)

The League Hierarchy file already existed at `Leagues/League Hierarchy.md` — the task note from 08:52 that it "may need a standalone write" was incorrect. It is a full standalone document. Updated it with:

- **MLS NEXT tier split values:** Added `mlsnext_homegrown = 160`, `mlsnext_academy = 135`, `mlsnext_unclassified = 147` as distinct rows with validation status (`validated` from win-rate data) and the "pending May 18–20" implementation note.
- **Validation status column:** Added across all Boys and Girls circuits — distinguishing `validated` (cross-league win-rate data), `theoretically sound; not Brier-validated`, and `assumed` entries.
- **GA ASPIRE Girls row:** Added at cal=100 with a note that the fix is `validated (post-fix pending)` — the overcalibration at 140 is known, fix execution is April 28.
- **GA ASPIRE Boys flag callout block:** Documented the uncertainty. Boys GA = 100 already. If Boys GA ASPIRE should sit below Boys GA (analogous to Girls: 140 → 100 for ASPIRE), the correct Boys GA ASPIRE value would be approximately 70. However, this is unvalidated — no Boys Brier analysis has been run to confirm the GA ASPIRE calibration differential for Boys specifically.

**Boys vs Girls calibration comparison table:**

| Circuit | Boys Cal Value | Girls Cal Value | Same? | Boys Value Source |
|---------|---------------|-----------------|-------|-------------------|
| MLS NEXT Homegrown | 160 | N/A | — | validated (win-rate) |
| MLS NEXT Academy | 135 | N/A | — | validated (win-rate) |
| ECNL | 120 | 130 | No | validated (baseline anchor) |
| GA | 100 | 140 | No | theoretically sound; not Brier-validated |
| GA ASPIRE | ~70? (unvalidated) | 100 | No | **unknown — needs Boys Brier analysis** |
| NPL/DPL/ECNL RL | 55 | 55 | Yes | validated |
| Pre-ECNL | 25 | 25 | Yes | validated |

**GA ASPIRE Boys implication:** Boys GA ASPIRE events (if any exist in the DB with "ASPIRE" in name since Feb 2025) are currently tagged as `ga` (cal=100 for Boys). If the April 28 GA ASPIRE fix uses a blanket name-pattern UPDATE (`event_name ILIKE '%ASPIRE%'`), it will reclassify Boys GA ASPIRE events alongside Girls ones. For Girls this is correct (140 → 100). For Boys, the result depends on whether a `ga_aspire` Boys tier is defined in `compute-rankings.js` — if not, those Boys events may fall to a default or error. This must be confirmed before the April 28 session. See Question for Presten below.

---

**2. Boys Calibration Option A — Interpretation Guide**
→ `Rankings/Boys Calibration Option A — Interpretation Guide.md`

Wrote the complete interpretation guide for the three minimum-viable Boys calibration queries from the Boys Gap Analysis. The document is self-contained: Presten runs the queries, reads this guide, and produces a Boys DSS go/no-go within 20 minutes.

Key thresholds written:

- **Query A (Top-10 U13B):** Healthy if ≥5/10 teams are recognizable national Boys clubs, ratings all >1,400, no single rating >1,700. Fails if top-10 contains only regional/recreational names or has an obvious >1,700 anomaly (merge artifact).
- **Query B (Boys vs Girls distribution):** Warning if average divergence >100 pts between genders or stddev divergence >125 pts. Some Boys-above-Girls divergence is expected (MLS NEXT at 160 pulls Boys top teams higher than Girls GA at 140). Boys team_count <100 = insufficient data for calibration evidence.
- **Query C (Game volume):** Healthy if MLS NEXT (U13+U14 Boys) ≥500 games, GA Boys ≥200 games. Below thresholds: Boys rankings in that tier are coverage-limited, not necessarily miscalibrated.
- **Section 4 Go/No-Go template:** Written to add to the Boys Gap Analysis doc after Presten runs the queries. Decision rules: All PASS = safe to show Boys at DSS. Any FAIL on Query A = do not show Boys rankings. Multiple WARNs = escalate to SENTINEL before May 14.

---

**3. MLS NEXT Tier Split — Calibration Application Spec**
→ `Rankings/MLS NEXT Tier Split — Calibration Application Spec.md`
→ `Rankings/MLS NEXT Tier Split — Calibration Spec 2026-04-24.md` (sensitivity note added)

Wrote the end-to-end application spec. Covers:

**Section 1 — Prerequisites:** Three mandatory checks before any change: (1) FORGE's classifier quality check (unclassified < 10% → proceed; 10–20% → partial split; ≥20% → hold). (2) Legacy `mlsnext` event count confirmed = 0 after FORGE's UPDATE. (3) Constants file path identified (`config/calibration.json` per prior docs — verify before session).

**Section 2 — Constants file diff:** Exact 3-line addition: `mlsnext_homegrown: 160`, `mlsnext_academy: 135`, `mlsnext_unclassified: 147`. Legacy `mlsnext: 160` kept as fallback. If the ranking engine uses JS rather than JSON, format adapts — values are the same.

**Section 3 — Post-split validation:**
- Confirm no legacy mlsnext events remain (COUNT = 0)
- Confirm no Boys top-20 team dropped below rank 50 in one recompute (40-point rating correction ≠ 30+ rank drop unless team had very few games)
- Confirm Homegrown avg_rating > Academy avg_rating (hypothesis validation from win-rate data)
- Confirm Girls U13G top-5 unchanged (split is Boys-only)

**Section 4 — Rollback:** Revert constants → revert event UPDATEs → re-run compute → verify. Copy-paste ready. Clean within 48 hours of application.

**Sensitivity note added to Calibration Spec:** `mlsnext_unclassified` recalculation formula included for any Academy/Homegrown distribution other than the assumed 60/40. E.g., if classifier shows 80% Academy → recalculate to 140, not 147.

---

## What I Found

**League Hierarchy file was already complete** — the concern from the 08:52 briefing ("may need a standalone write") was unfounded. The file exists and has Boys entries. It was missing the MLS NEXT tier split details and validation status, which are now added.

**Boys GA ASPIRE is the open ambiguity.** The GA ASPIRE Girls fix (April 28) changes events tagged as `ga` with "ASPIRE" in the name to `ga_aspire`. If the UPDATE is gender-agnostic (likely, since event_tiers doesn't store gender), Boys GA ASPIRE events get reclassified too. This is structurally correct (Boys GA ASPIRE should be a sub-tier of Boys GA), but the calibration value for Boys GA ASPIRE is not defined anywhere in the vault. If `compute-rankings.js` receives a Boys event with `event_tier = 'ga_aspire'` and has no Boys-specific calibration for that tier, it will either: (a) use the Girls value (100 — which happens to equal Boys GA anyway, so no practical impact), or (b) fall through to a default. This needs to be confirmed.

**MLS NEXT Application Spec constants file path assumed.** I referenced `config/calibration.json` based on the Calibration Production Runbook. The Phase 8 calibration map may actually be inline in `compute-rankings.js`. Presten should verify before the May 18–20 session — the Application Spec's Section 1.3 has the grep command to locate it.

---

## In Progress

Nothing. All writable tasks are complete.

**Pending-execution tasks (require Presten psql):**
- `task-2026-04-23-ga-aspire-tier-fix` — spec complete, UPDATE SQL ready in `ga-coverage-audit-results-2026-04-23.md`. Deadline May 14.
- `task-2026-04-23-u13-u14-calibration-fix` — pre-deploy checklist and validation results written. Deployment window May 18–20 (per Calibration Production Runbook). Do NOT deploy before May 17.

---

## Questions for Presten

**Q1 (Priority: HIGH — needed before April 28):**

The GA ASPIRE tier fix session is scheduled for April 28. The UPDATE SQL reclassifies events from `ga` → `ga_aspire` based on event name containing "ASPIRE." This will affect both Girls and Boys GA ASPIRE events if any exist.

**Does `compute-rankings.js` have a Boys-specific calibration value for `ga_aspire`?** Or does it fall through to the Girls value (100)?

- If Boys `ga_aspire` maps to 100 (same as Boys GA): no practical impact — Boys GA and Boys GA ASPIRE would both be cal=100.
- If Boys `ga_aspire` is undefined in the calibration map: the Boys GA ASPIRE events get reclassified but produce no rating change (or an error). This should be checked before April 28.
- If Boys should have a distinct `ga_aspire` value (e.g., 70): ELO needs to define it before the April 28 session. Based on current data, there is no Boys Brier analysis to validate a Boys GA ASPIRE value, so 70 would be a design assumption, not a validated number.

**Recommended action:** Before April 28, run:
```sql
SELECT event_tier, COUNT(*) AS games
FROM games g
JOIN events e ON e.id = g.event_id
WHERE e.event_name ILIKE '%ASPIRE%'
  AND e.event_tier = 'ga'
GROUP BY g.gender, event_tier;
```
If Boys GA ASPIRE games are present: confirm the calibration map handles `ga_aspire` for Boys before running the UPDATE.

---

## Tomorrow's Plan

1. Check Tasks/ for new assignments from SENTINEL.
2. If Rank Bands implementation session has run by then: review threshold validation query results from `Rank Bands Threshold Validation — 2026-04-24.md` — if any threshold fails the pass/fail criteria, ELO needs to propose revised thresholds.
3. After the April 28 calibration session: confirm GA ASPIRE fix completed and that Boys GA ASPIRE was handled correctly (or flagged for follow-up).
4. Monitor FORGE for MLS NEXT classifier Step A output — once available, ELO checks whether the 60/40 Academy/Homegrown assumption holds and whether `mlsnext_unclassified = 147` needs recalculation.
5. Begin Boys Option A validation prep: confirm the Section 4 Go/No-Go template in the Interpretation Guide is compatible with the May 14 DSS Data Readiness Validation session format.

---

_Tasks completed: 3 | Deliverables written: 2 (+1 updated in-place, +1 sensitivity note) | Questions posted: 1 | Flags: 1 (Boys GA ASPIRE session scope — confirm before April 28)_

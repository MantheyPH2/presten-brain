---
type: agent-briefing
agent: FORGE
date: 2026-04-24
session: 02:07
status: completed
tasks_completed: 3
tasks_in_progress: 3
tasks_blocked: 1
questions_posted: 0
flags: []
---

# FORGE Briefing — 2026-04-24 02:07

---

## What I Did Today

Completed all three pending vault tasks assigned for 2026-04-24:

1. **ECNL Dedup `verified = true` Fix Note (P0)** — `Reports/ecnl-dedup-verified-fix-2026-04-24.md`
2. **MLS NEXT Event Classifier Queries** — `Rankings/MLS NEXT Event Classifier — Queries 2026-04-24.md`
3. **SnapSoccer Ingestion MVP Design** — `Data Sources/SnapSoccer Ingestion MVP — Design 2026-04-24.md`

All three task files marked `completed`.

---

## What I Found

### ECNL Dedup (P0)

`scripts/ecnl-transition-dedup.js` does not exist yet — it's on the May 1 critical path. The P0 risk is therefore a spec enforcement issue, not an existing code bug. Filed `Reports/ecnl-dedup-verified-fix-2026-04-24.md` as the authoritative INSERT spec: every `team_merges` INSERT in the script must include `verified = true, verified_by = 'ECNL_TRANSITION_2026'`. The `verified` column is confirmed present in `team_merges` (18,684 of 27,820 existing rows are verified — the Ranking Engine already relies on it). **ELO's P0 flag is closed proactively.** No action needed from Presten before May 1.

### MLS NEXT Event Classifier

Wrote the full SQL playbook (Steps A–C + UPDATE + recalibration UPDATE for `compute-rankings.js`). Key decisions:
- 9 Homegrown patterns, 5 Academy patterns — built from the existing `classify-mlsnext-events.js` script (ELO, April 23) and the Tier Split Analysis Section 4
- Decision rule explicit: if Step A shows ≥10% of mlsnext game volume is unclassified, STOP and flag to ELO before running the UPDATE
- Calibration map addition: `mlsnext_homegrown: 160`, `mlsnext_academy: 135`, `mlsnext_unclassified: 147`
- **Apply in the May 18–20 window only**, not before DSS
- Fallback queries included if no `event_tiers` table exists (targeting `games.event_tier` directly)

### SnapSoccer Ingestion MVP

**Access path confirmed: Option A — HTML scrape of `soccer.sincsports.com/schedule.aspx`** (no auth, no rate limits, server-rendered HTML, same platform as our existing 34,961 Sinc Sports games).

Key design decisions:
- Script: `scripts/ingest-snapsoccer.js` following `crawl-gotsport-api-parallel.js` pattern
- `source = 'snapsoccer'`, `source_priority = 5` (same dedup tier as Sinc Sports)
- `source_id = snapsoccer_{tid}_{gameId}` — deterministic composite if no stable ID in HTML
- Team matching: fuzzy Pass 1 → insert new team Pass 2; never skip games on no-match
- Playdate TIDs excluded (no scores by design)
- **Coverage:** ~10,000–20,000 historical games
- **Effort:** 5–9 hours for test run; 8–12 hours for full production ingest
- **Go/No-Go: Conditional Go, after Org-ID Expansion.** The Org-ID expansion delivers more games (20K–35K) with zero new code — do that first. SnapSoccer is viable for a May 5–8 session if time allows.

---

## In Progress (Pending Presten / Code Access)

| Task | Status | What's Blocking |
|---|---|---|
| Option B Exponential Backoff (`crawl-gotsport-api-parallel.js`) | vault_deliverable complete (`Reports/backoff-implementation-2026-04-24.md`) | Presten implements code |
| GotSport Org-ID Expansion (USYS state associations) | vault_deliverable complete (`Reports/gotsport-org-expansion-test-2026-04-24.md`) | Presten runs discovery queries + config update |
| Daily Pipeline (`run-daily.sh`, `get-active-team-ids.sh`) | vault_deliverable complete (spec in `Daily Pipeline Cadence Scope.md`) | Presten implements scripts; due May 1 |

---

## Blocked

| Task | Blocked By |
|---|---|
| USA Rank Accuracy Audit SQL | Requires psql access to `youth_soccer` at localhost:5432. All SQL pre-written in `Rankings/USA Rank Ranking Accuracy Audit — April 2026.md`. 30-minute task for Presten. Tables: U13G + U14G, 16 teams vs USA Rank. Football Academy NJ merge question is the key finding needed. |

---

## Questions for Presten

None new. One standing:

- **USA Rank Accuracy Audit (blocked):** Presten has all the SQL. This was due 2026-04-24 and is the last thing blocking calibration validation for ELO. If Presten can run those 16 team lookups at any point, it unblocks ELO's comparison table and the Football Academy NJ merge question.

---

## Tomorrow's Plan

1. **If backoff fix is deployed:** Run 540 confirmed rate-limit-casualty team backfill (not all 847 — the targeted subset from `Reports/stale-teams-2026-04-21.csv`). Document results.
2. **Begin monitoring** Org-ID expansion test run once Presten confirms config changes are in.
3. **May 1:** Begin `ecnl-transition-dedup.js` per the `Reports/ecnl-dedup-verified-fix-2026-04-24.md` spec.
4. **If time:** SnapSoccer test run on 2–3 competitive tournament IDs (Coastal Soccer Invitational, Coastal Academy Cup).

---

*FORGE — 2026-04-24 02:07*
